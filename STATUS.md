# STATUS — 2brain-конвейер (репо zcode-sync)

Обновлено: 2026-09-15 00:30. Живое состояние машины — STATE.md в репо
`C:\Users\Us\.zcode\workspace\default`; этот файл — про этот репозиторий.

## Итог дня (2026-09-14)

Прогон очереди SBER (129 задач, 19:21→23:41, лог `%TEMP%/sber_worker3.log`)
завершён штатно: 110 результатов доставлено на homelab (включая 11 seed),
19 доставлены раньше, 1 PDF падал с «Cycle detected in outlines» — его
переобработала параллельная retry-сессия. Homelab-демон всё забрал:
в `_llm/notes` к 00:25 — 188 extracted + 3 queued, queued-gpu = 0 (было 130).
Первый боевой прогон нового кода (hourly 00:12) отработал корректно:
после drop_sent_jobs очередь оказалась пуста (только служебный
`_held_drawings` без манифеста), воркер не брался, чистки не было.

## Последний этап (завершён)

Постоянный каталог результатов в run_queue — коммит **6aaf6e6**:

- `win-worker/run_queue.py`: tmp на прогон заменён на постоянный
  `win-worker/work/` (jobs, results, *.tar). Прогон, убитый жёстко, больше
  не теряет результаты: следующий прогон сам подхватит их из work/results
  (skip-if-done воркера) — ручной `--seed-results` для resume больше не
  нужен (флаг оставлен для совместимости).
- `drop_sent_jobs()`: задания со статусом sent в скачанном tar удаляются —
  доставленное не перезапускается повторно (раньше каждый прогон гонял весь
  tar заново, включая sent).
- `cleanup_after_delivery()`: после успешной доставки удаляются доставленные
  результаты + jobs целиком; при падении воркера ничего не чистится.
- `win-worker/work/` в `2brain/.gitignore`.
- Тесты: `2brain/tests/test_queue_results.py` (3 новых), всего 22/22:
  `C:/Users/Us/2brain-worker/venv/Scripts/python.exe -m unittest discover -s 2brain/tests`
  (из корня репо).

Предыдущий этап — gpu-lock/resume, коммит **05aa5e2** (инцидент 2026-09-14:
два оркестратора поделили RTX 3050 → VRAM 92%, 0 вывода; 15 готовых
результатов спасено в `C:\Users\Us\2brain-worker\resume\2026-09-14\`).

## Что дальше (следующий этап)

1. Очередь пуста (0 queued-gpu, служебный jobs на homelab чист). Утром
   2026-09-15 10:00 будни — батч «Карт» (2brain-sutya) по 101 заметке
   со статусом queued (188 extracted уже с Картами).
2. Новые PDF — в `_Drop` локального ваулта → `run_local.py` (результат на
   homelab, без кругового перегона). DJVU/видео — пока старым путём.
3. Расхождение копий: `C:\Users\Us\2brain-worker\run_queue.py` — устаревшая
   scp-версия (root@100.108.62.152, доставка через Syncthing), реально
   планировщик и прогон используют версию из этого репо
   (`win-worker/run_queue.py`, root@192.168.2.9, tar). Копию в
   2brain-worker либо удалить, либо синхронизировать — чтобы не путать
   следующих агентов.
