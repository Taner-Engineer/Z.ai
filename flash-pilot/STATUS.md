# STATUS: 2brain — «Суть» вычистлена полностью, формат один: «Карта» (2026-09-10)

Сессия, не видевшая прошлой переписки: читай это + `git log --oneline -8`.

## Итоговое состояние

1. **Указание владельца**: в хранилище остаётся ТОЛЬКО «## Карта» — структурный
   указатель для навигации агента без перечитывания полного текста; «Суть»
   (пересказ-тезисы) ликвидируется везде. Выполнено по всей вертикали:
   - **7 заметок**: СП 56 (14 пунктов), СП 155 (14), СП 252 (13, статус
     queued→extracted), How-to-Claude-чертежи (1 строка), Me at the zoo
     (1 строка), Syncthing FAQ (1 строка) — раздел «## Суть» заменён на
     «## Карта» (черновики — субагент по flash-delegate, спека
     `flash-pilot/TASK2.md`, черновики+сверка — `flash-pilot/karta-drafts/`,
     скрипт замены — `flash-pilot/apply_karta.py`, приёмка — сверка с сырьём);
     Hermes-заметка (queued, пустая) — переименован только заголовок раздела,
     статус остался queued (извлечения ещё не было);
   - **шаблон** `_Templates/Заметка.md`: заглушка теперь «## Карта»;
   - **демон**: `vault.py` (render_stub + create_note) и комментарий `daemon.py`
     — Суть→Карта; деплой на homelab /opt/2brain (scp), коммит там `240c6af`;
     13 unittest зеркала — зелёные (тестов на строку «Суть» не было);
   - **скилл v1** («Суть», понедельники) в `C:\Users\Us\.agents\skills\2brain-sutya\`
     перезаписан актуальным v2 «Карта» (копия .zcode, md5 совпал с
     zcode-sync/skills) — второй писатель «Сутьей» устранён.
2. Проверка: `grep -rn "## Суть"` по vault — 0 вхождений в Knowledge/Books/
   Projects/шаблоне (единственный остаток — конфликт-копия мёртвого скилла,
   см. «Известные мелочи»).
3. Предыдущий этап (Уманский, Карта 23 раздела, extracted) — коммит 33a3f05.

## Deploy-состояние

- Homelab /opt/2brain: vault.py + daemon.py обновлены и закоммичены (240c6af).
- Правки заметок уйдут на мастер Syncthing'ом сами; git на мастере — не наша
  сторона (как и прежде).

## Известные мелочи

- **Не чищено без указания владельца**: (а) sync-conflict-обломки мёртвых
  скиллов в vault `.claude/skills/extract/` и `triage/` (файлы
  `*.sync-conflict-20260909-*`; в extract-обломке упоминается Суть — он
  инертен); (б) старые «Суть»-выжимки доступны в истории git мастера vault,
  при необходимости откатнуть; (в) заметка Syncthing FAQ — старая схема
  frontmatter с 11-м полем `notebook_id` (доктрина: ровно 10) — чистить через
  /vault-calibrate.
- **Дрейф на homelab**: в /opt/2brain есть НЕзакономмиченные изменения
  config.py, processors.py, router.py, gpu-worker/worker.py, Dockerfile и
  удалённый convert_one.py (видимо, правки этапов 09-09/09-10 без коммитов;
  runtime актуален). Мой коммит 240c6af их НЕ включает. При случае —
  закоммитить дрейф или сверить с зеркалом.
- В очереди 1 queued-заметка: «Hermes Agent Zero…» (Карта пустая, ждёт батча).

## Что дальше (опции)

- Батч Карт по Hermes-заметке (последняя в очереди).
- Разобрать дрейф /opt/2brain (см. выше) и конфликт-обломки скиллов.
- Чистка homelab: docling:cpu-образ, /opt/2brain/model_cache, старые jobs.
- Хвосты из HANDOFF.md: RDP-перезагрузка ПК, ffmpeg для STT, баги Hermes.
